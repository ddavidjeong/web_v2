import React from "react";
import { ThemeProvider } from "../components/ThemeContext";

const Contact = () => {
  return (
    <ThemeProvider>
      <div> About Me</div>
    </ThemeProvider>
  );
};

export default Contact;
