import React from "react";
import { ThemeProvider } from "../components/ThemeContext";

const About = () => {
  return (
    <ThemeProvider>
      <div> About Me</div>
    </ThemeProvider>
  );
};

export default About;
